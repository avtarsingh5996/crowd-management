{
  "name": "data-retriever",
  "version": "1.0.0",
  "description": "Lambda function for retrieving crowd data",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.425.0",
    "@aws-sdk/lib-dynamodb": "^3.425.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^18.18.0",
    "typescript": "^4.9.5"
  }
} 